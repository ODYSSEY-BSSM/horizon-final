import type { ProblemCreateRequest, ProblemResponse, ProblemSolveRequest } from '../types';
import { mockProblemApi } from '@/shared/api/mock/mockRoadmapApi';

// Mock API 사용 (실제 API 대신)
export const problemApi = mockProblemApi;
