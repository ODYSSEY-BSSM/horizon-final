# 환경 변수 템플릿
# .env.local 파일을 생성하고 아래 내용을 복사하세요

# API Base URL (백엔드 서버 주소)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# WebSocket Base URL (STOMP over WebSocket)
# HTTP일 때는 ws://, HTTPS일 때는 wss://
NEXT_PUBLIC_WS_BASE_URL=ws://localhost:8080

# 개발용 인증번호 (이 번호는 무조건 통과됨)
NEXT_PUBLIC_DEV_VERIFICATION_CODE=080531

# 프로덕션 환경 예시:
# NEXT_PUBLIC_API_BASE_URL=https://api.yourdomain.com
# NEXT_PUBLIC_WS_BASE_URL=wss://api.yourdomain.com
